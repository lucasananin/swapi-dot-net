@model Swapi.Models.ViewModels.PaginationViewModel

<nav class="pagination">

    @if (Model.HasPrevious)
    {
        <a asp-route-resource="@Model.Resource" asp-route-search="@Model.Search" asp-route-page="@(Model.CurrentPage - 1)">
            Previous
        </a>
    }

    @foreach (var _page in Model.GetVisiblePages())
    {
        if (_page == Model.CurrentPage)
        {
            <strong>@_page</strong>
        }
        else
        {
            <a asp-route-resource="@Model.Resource" asp-route-search="@Model.Search" asp-route-page="@_page">
                @_page
            </a>
        }
    }

    @if (Model.HasNext)
    {
        <a asp-route-resource="@Model.Resource" asp-route-search="@Model.Search" asp-route-page="@(Model.CurrentPage + 1)">
            Next
        </a>
    }

</nav>